using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputManager : BasicMonoSingle<InputManager>
{
    // Update is called once per frame
    void Update()
    {
        // if (Input.GetKey(KeyCode.A))
        // {
        //     ClientManager.instance.Send(EInputEnum.leftDir);
        // }else if (Input.GetKey(KeyCode.D))
        // {
        //     ClientManager.instance.Send(EInputEnum.rightDir);
        // }else if (Input.GetKey(KeyCode.W))
        // {
        //     ClientManager.instance.Send(EInputEnum.upDir);
        // }else if (Input.GetKey(KeyCode.S))
        // {
        //     ClientManager.instance.Send(EInputEnum.downDir);
        // }
        // else
        // {
        //     ClientManager.instance.Send(EInputEnum.none);
        // }
    }
}
